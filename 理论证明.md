# 理论证明：梯度对齐表征去偏与置信度公平性诊断

## 宏观证明链路

### 第一部分：梯度对齐 → 表征偏差 → 公平性

```
现实问题：挑战2——表征偏差引起的公平性退化
    ↓
直觉：减小表征偏差(Δ_rep) → 改善公平性(EO)
    ↓
能否直接证明"减小Δ_rep → 改善EO"？
    → 能。定理1：EO ≤ C · Δ_rep
    → 依赖假设：线性分类头 + sub-Gaussian + 有界协方差差异
    ↓
如何减小Δ_rep？
    → 方案：梯度对齐正则化 L_grad = 1 - ρ_l
    ↓
能否证明"梯度对齐 → 减小Δ_rep"？
    → 不能直接证明"单调减小"，但能证明"变化幅度受约束"
    → 引理1：‖g₀-g₁‖² = ‖g₀‖² + ‖g₁‖² - 2‖g₀‖‖g₁‖ρ_l（代数恒等式）
    → 定理2：Δ_rep的变化 = -2η·v·u + η²‖u‖²（精确等式，非放缩）
    → ρ_l越高 → ‖u‖越小 → Δ_rep的幅度项越小
    ↓
梯度对齐正则化会破坏收敛吗？
    → 引理2：不会。L_grad光滑，总损失仍收敛。
    ↓
串联（定理3·主定理）：
    梯度对齐 → 约束‖u‖ → 约束Δ_rep变化幅度 → 约束EO变化幅度
    结合定理1(EO ≤ C·Δ_rep) → EO的上界受约束
```

### 第二部分：置信度 → 决策边界 → 公平性

```
现实问题：如何诊断哪些群组存在表征偏差？
    ↓
直觉：置信度可以作为诊断信号
    ↓
能否证明"置信度差异 → EO差距"？
    → 定理4：c = σ(‖w‖·d)（置信度与决策边界距离单调递增）
    → 引理3：决策边界距离差异 → 预测概率差异 → EO差距
    → 串联（定理5）：置信度差异Δ_c → 决策边界距离差异Δ_d → EO差距
```

---

## 一、符号定义

| 符号 | 含义 |
|------|------|
| $\mathcal{X}, \mathcal{Y}, \mathcal{G}$ | 输入空间、标签空间 $\{0,1\}$、敏感群组空间 $\{0,1\}$ |
| $f_\phi: \mathcal{X} \to \mathbb{R}^d$ | 特征提取器（encoder），参数为 $\phi$ |
| $h_\psi: \mathbb{R}^d \to \mathbb{R}$ | 线性分类头，参数为 $\psi = (w, b)$，即 $h_\psi(z) = w^\top z + b$ |
| $z = f_\phi(x) \in \mathbb{R}^d$ | 输入 $x$ 的特征表示 |
| $\hat{p} = \sigma(w^\top z + b)$ | 预测概率，$\sigma(t) = 1/(1+e^{-t})$ |
| $\ell(\hat{p}, y)$ | 二元交叉熵损失 |
| $\mathcal{L}_{\text{task}}(x, y)$ | 单样本任务损失 |
| $\nabla_z \mathcal{L}_{\text{task}}$ | 任务损失对特征 $z$ 的梯度 |

**定义1（表征偏差）**：对于标签 $l$，群组间的表征偏差为：
$$\Delta_{\text{rep}}^{(l)} = \| \mathbb{E}[z \mid g=0, y=l] - \mathbb{E}[z \mid g=1, y=l] \|_2$$

**定义2（Equalized Odds 差距）**：对于标签 $l$：
$$\text{EO}^{(l)} = | P(\hat{y}=1 \mid g=0, y=l) - P(\hat{y}=1 \mid g=1, y=l) |$$

**定义3（群组梯度原型）**：对于群组 $g$ 和标签 $l$：
$$\bar{g}_{g,l} = \mathbb{E}[\nabla_z \mathcal{L}_{\text{task}}(x, y) \mid g, l] \in \mathbb{R}^d$$

**定义4（梯度方向一致性）**：对于标签 $l$：
$$\rho_l = \frac{\bar{g}_{g0,l} \cdot \bar{g}_{g1,l}}{\|\bar{g}_{g0,l}\| \cdot \|\bar{g}_{g1,l}\|} \in [-1, 1]$$

**定义5（置信度）**：对于 logit 为 $s = w^\top z + b$ 的样本：
$$c = \max(\hat{p}, 1-\hat{p}) = \sigma(|s|)$$

**定义6（决策边界距离）**：对于特征为 $z$ 的样本：
$$d = \frac{|w^\top z + b|}{\|w\|}$$

---

## 二、前置假设

**假设1（线性分类头）**：分类头 $h_\psi$ 是线性的：$h_\psi(z) = w^\top z + b$。

> **注**：此假设与 PDFFed 架构自洽。在 PDFFed 中，原型通过 `only_clf_forward` 传入最后一层线性层。encoder $f_\phi$ 可以是任意复杂模型（如 BERT），线性假设仅适用于操作原型的分类头。

**假设2（sub-Gaussian 特征）**：对于每个群组 $g$ 和标签 $l$，特征分布 $z \mid g, y=l$ 是 sub-Gaussian 的，均值为 $\mu_{g,l}$，协方差矩阵为 $\Sigma_{g,l}$。

**假设3（有界协方差差异）**：存在常数 $\Delta_\Sigma$ 使得：
$$\|\Sigma_{g0,l} - \Sigma_{g1,l}\|_{\text{op}} \leq \Delta_\Sigma, \quad \forall l$$
当 $\Delta_\Sigma = 0$ 时，退化为共享协方差假设。

**假设4（有界特征范数）**：存在常数 $\sigma_{\max}$ 使得：
$$\sqrt{w^\top \Sigma_{g,l} w} \leq \sigma_{\max}, \quad \forall g, l$$

---

## 三、定理与证明：表征偏差与公平性

### 定理1（表征偏差界住 EO 差距）

在假设1-4下，EO 差距满足：
$$\text{EO}^{(l)} \leq \frac{\|w\|}{\sqrt{2\pi}\,\sigma_{\max}} \cdot \Delta_{\text{rep}}^{(l)} + O\!\left(\frac{\|w\|^2 \Delta_\Sigma}{\sigma_{\max}^3}\right)$$

当 $\Delta_\Sigma = 0$（共享协方差）时，简化为：
$$\text{EO}^{(l)} \leq \frac{\|w\|}{\sqrt{2\pi}\,\sigma} \cdot \Delta_{\text{rep}}^{(l)}$$

### 定理1 证明

**Step 1：用特征分布表达 EO**

群组条件预测概率为：
$$P(\hat{y}=1 \mid g, y=l) = \mathbb{E}_{z \sim \mathcal{D}_{g,l}}[\sigma(w^\top z + b)]$$

**Step 2：利用 sub-Gaussian 集中不等式**

令 $s = w^\top z + b$，则 $s \mid g, y=l$ 是 sub-Gaussian 的，均值 $\mu'_g = w^\top \mu_{g,l} + b$，方差 $\sigma_g^2 = w^\top \Sigma_{g,l} w$。

由 sub-Gaussian 性质，$s$ 的 CDF 用 $\Phi(s/\sigma_g)$ 近似，Berry-Esseen 型误差为 $O(1/\sqrt{n})$：
$$P(\hat{y}=1 \mid g, y=l) \approx \Phi\!\left(\frac{\mu'_g}{\sigma_g}\right)$$

**Step 3：用中值定理界住 EO 差距**

$$\text{EO}^{(l)} = \left|\Phi\!\left(\frac{\mu'_{g0}}{\sigma_{g0}}\right) - \Phi\!\left(\frac{\mu'_{g1}}{\sigma_{g1}}\right)\right|$$

由中值定理，存在 $\xi$ 使得：
$$\text{EO}^{(l)} = \phi(\xi) \cdot \left|\frac{\mu'_{g0}}{\sigma_{g0}} - \frac{\mu'_{g1}}{\sigma_{g1}}\right|$$

其中 $\phi$ 是标准正态 PDF，$\phi(\xi) \leq 1/\sqrt{2\pi}$。

**Step 4：共享协方差情形（$\sigma_{g0} = \sigma_{g1} = \sigma$）**

$$\text{EO}^{(l)} \leq \frac{1}{\sqrt{2\pi}} \cdot \frac{|w^\top(\mu_{g0,l} - \mu_{g1,l})|}{\sigma}$$

由 Cauchy-Schwarz 不等式：
$$|w^\top(\mu_{g0,l} - \mu_{g1,l})| \leq \|w\| \cdot \|\mu_{g0,l} - \mu_{g1,l}\| = \|w\| \cdot \Delta_{\text{rep}}^{(l)}$$

因此：
$$\text{EO}^{(l)} \leq \frac{\|w\|}{\sqrt{2\pi}\,\sigma} \cdot \Delta_{\text{rep}}^{(l)}$$

**Step 5：有界协方差差异情形（$\sigma_{g0} \neq \sigma_{g1}$）**

将 $\sigma_{g0} = \sigma + \delta_0$，$\sigma_{g1} = \sigma + \delta_1$ 展开，其中 $|\delta_g| \leq \Delta_\Sigma/(2\sigma)$。通过 Taylor 展开：

$$\left|\frac{\mu'_{g0}}{\sigma_{g0}} - \frac{\mu'_{g1}}{\sigma_{g1}}\right| \leq \frac{\|w\| \cdot \Delta_{\text{rep}}^{(l)}}{\sigma} + O\!\left(\frac{\|w\|^2 \Delta_\Sigma}{\sigma^3}\right)$$

结合 PDF 上界：
$$\text{EO}^{(l)} \leq \frac{\|w\|}{\sqrt{2\pi}\,\sigma_{\max}} \cdot \Delta_{\text{rep}}^{(l)} + O\!\left(\frac{\|w\|^2 \Delta_\Sigma}{\sigma_{\max}^3}\right)$$

$\square$

---

## 四、引理与定理：梯度对齐与表征偏差

### 引理1（梯度差范数与方向一致性的关系）

令 $u_l = \bar{g}_{g0,l} - \bar{g}_{g1,l}$ 为梯度差向量，则：
$$\|u_l\|^2 = \|\bar{g}_{g0,l}\|^2 + \|\bar{g}_{g1,l}\|^2 - 2\|\bar{g}_{g0,l}\|\|\bar{g}_{g1,l}\|\rho_l$$

推论：
- 当 $\rho_l = 1$：$\|u_l\| = |\|\bar{g}_{g0,l}\| - \|\bar{g}_{g1,l}\||$（最小值）
- 当 $\rho_l = -1$：$\|u_l\| = \|\bar{g}_{g0,l}\| + \|\bar{g}_{g1,l}\|$（最大值）

### 引理1 证明

直接展开平方范数：
$$\|u_l\|^2 = \|\bar{g}_{g0,l} - \bar{g}_{g1,l}\|^2 = \|\bar{g}_{g0,l}\|^2 + \|\bar{g}_{g1,l}\|^2 - 2\,\bar{g}_{g0,l} \cdot \bar{g}_{g1,l}$$

最后一步利用 $\rho_l$ 的定义：$\bar{g}_{g0,l} \cdot \bar{g}_{g1,l} = \|\bar{g}_{g0,l}\|\|\bar{g}_{g1,l}\|\rho_l$。

$\square$

### 定理2（梯度下降下的表征偏差动力学）

令 $\mu_{g,l}^{(t)}$ 为第 $t$ 步的群组均值，$v_l^{(t)} = \mu_{g0,l}^{(t)} - \mu_{g1,l}^{(t)}$ 为表征偏差向量。在学习率为 $\eta$ 的梯度下降下，**平方**表征偏差的演化满足**精确等式**：

$$\left(\Delta_{\text{rep}}^{(l,t+1)}\right)^2 - \left(\Delta_{\text{rep}}^{(l,t)}\right)^2 = -2\eta\, v_l^{(t)} \cdot u_l + \eta^2 \|u_l\|^2$$

其中 $u_l = \bar{g}_{g0,l} - \bar{g}_{g1,l}$。

由引理1，第二项（幅度项）为：
$$\eta^2 \|u_l\|^2 = \eta^2\!\left(\|\bar{g}_{g0,l}\|^2 + \|\bar{g}_{g1,l}\|^2 - 2\|\bar{g}_{g0,l}\|\|\bar{g}_{g1,l}\|\rho_l\right)$$

该项关于 $\rho_l$ **严格递减**。

### 定理2 证明

**Step 1：特征更新规则**

$$z^{(t+1)} = z^{(t)} - \eta \cdot \nabla_z \mathcal{L}_{\text{task}}(x, y)$$

**Step 2：群组均值更新**

对群组 $g$、标签 $l$ 取期望：
$$\mu_{g,l}^{(t+1)} = \mu_{g,l}^{(t)} - \eta \cdot \bar{g}_{g,l}$$

**Step 3：表征偏差向量更新**

$$v_l^{(t+1)} = \mu_{g0,l}^{(t+1)} - \mu_{g1,l}^{(t+1)} = v_l^{(t)} - \eta\, u_l$$

**Step 4：平方范数展开（精确等式，无放缩）**

$$\|v_l^{(t+1)}\|^2 = \|v_l^{(t)} - \eta\, u_l\|^2 = \|v_l^{(t)}\|^2 - 2\eta\, v_l^{(t)} \cdot u_l + \eta^2 \|u_l\|^2$$

因此：
$$\left(\Delta_{\text{rep}}^{(l,t+1)}\right)^2 - \left(\Delta_{\text{rep}}^{(l,t)}\right)^2 = -2\eta\, v_l^{(t)} \cdot u_l + \eta^2 \|u_l\|^2$$

**Step 5：代入引理1**

$$\eta^2 \|u_l\|^2 = \eta^2\!\left(\|\bar{g}_{g0,l}\|^2 + \|\bar{g}_{g1,l}\|^2 - 2\|\bar{g}_{g0,l}\|\|\bar{g}_{g1,l}\|\rho_l\right)$$

对 $\rho_l$ 求偏导：
$$\frac{\partial (\eta^2 \|u_l\|^2)}{\partial \rho_l} = -2\eta^2 \|\bar{g}_{g0,l}\|\|\bar{g}_{g1,l}\| < 0$$

因此 $\eta^2\|u_l\|^2$ 关于 $\rho_l$ 严格递减。 $\square$

> **注**：定理2给出了**精确等式**（不是不等式），表征偏差的变化分为两项：
> - **方向项** $(-2\eta\, v \cdot u)$：取决于当前偏差方向 $v$ 与梯度差 $u$ 的对齐程度，可正可负。
> - **幅度项** $(\eta^2\|u\|^2)$：恒非负，且关于 $\rho_l$ 严格递减。
>
> 梯度对齐正则化 $\mathcal{L}_{\text{grad}} = 1 - \rho_l$ 通过最大化 $\rho_l$ 来最小化幅度项，从而约束表征偏差的变化速率。

### 引理2（梯度对齐正则化的收敛性）

令总损失为 $\mathcal{L} = \mathcal{L}_{\text{task}} + \lambda \mathcal{L}_{\text{grad}}$，其中 $\mathcal{L}_{\text{grad}} = 1 - \rho_l$，$\lambda > 0$。若 $\mathcal{L}_{\text{task}}$ 是 $L$-光滑的，$\mathcal{L}_{\text{grad}}$ 是 $M$-光滑的，则对于学习率 $\eta \leq 1/(L + \lambda M)$，梯度下降收敛：

$$\mathcal{L}^{(t+1)} - \mathcal{L}^{(t)} \leq -\eta\!\left(1 - \frac{\eta(L+\lambda M)}{2}\right) \|\nabla \mathcal{L}^{(t)}\|^2$$

### 引理2 证明

这是光滑优化的标准结果。$\mathcal{L}_{\text{task}}$ 是 $L$-光滑的，$\mathcal{L}_{\text{grad}}$ 是 $M$-光滑的，所以总损失 $\mathcal{L}$ 是 $(L + \lambda M)$-光滑的。由下降引理：

$$\mathcal{L}^{(t+1)} \leq \mathcal{L}^{(t)} - \eta\|\nabla \mathcal{L}^{(t)}\|^2 + \frac{(L+\lambda M)\eta^2}{2}\|\nabla \mathcal{L}^{(t)}\|^2$$

整理即得。当 $\eta \leq 1/(L+\lambda M)$ 时，系数非负，保证下降。 $\square$

### 定理3（主定理：梯度对齐约束 EO 差距）

在假设1-4下，加入梯度对齐正则化 $\mathcal{L}_{\text{grad}} = 1 - \rho_l$，学习率满足引理2，收敛时的 EO 差距满足：

$$\text{EO}^{(l, \infty)} \leq \frac{\|w\|}{\sqrt{2\pi}\,\sigma_{\max}} \cdot \Delta_{\text{rep}}^{(l, \infty)} + O\!\left(\frac{\|w\|^2 \Delta_\Sigma}{\sigma_{\max}^3}\right)$$

梯度对齐正则化通过以下机制约束 $\Delta_{\text{rep}}^{(l, \infty)}$：
1. 最大化 $\rho_l$ → 最小化 $\|u_l\|$（引理1）→ 最小化幅度项 $\eta^2\|u_l\|^2$（定理2）
2. 保证总损失收敛（引理2）→ 系统到达稳定状态

### 定理3 证明

由定理1，任意步 $t$：
$$\text{EO}^{(l,t)} \leq C \cdot \Delta_{\text{rep}}^{(l,t)} + \epsilon$$

其中 $C = \|w\|/(\sqrt{2\pi}\,\sigma_{\max})$，$\epsilon = O(\|w\|^2 \Delta_\Sigma / \sigma_{\max}^3)$。

由引理2，总损失 $\mathcal{L}$ 收敛到稳定点 $\nabla \mathcal{L} \approx 0$。

在收敛时，梯度 $\bar{g}_{g,l} \to 0$，因此 $u_l \to 0$，偏差动力学稳定：$(\Delta_{\text{rep}}^{(t+1)})^2 - (\Delta_{\text{rep}}^{(t)})^2 \to 0$。

在训练过程中，梯度对齐正则化确保 $\rho_l$ 被最大化，从而每步的 $\|u_l\|$ 最小化（引理1）。更小的 $\|u_l\|$ 意味着：
- 幅度项 $\eta^2\|u_l\|^2$ 更小，限制 $\Delta_{\text{rep}}$ 的增长
- 方向项 $-2\eta\, v \cdot u$ 的绝对值也更小（因为 $\|u_l\|$ 更小），限制 $\Delta_{\text{rep}}$ 的双向变化

因此，收敛时的表征偏差 $\Delta_{\text{rep}}^{(l,\infty)}$ 受梯度对齐约束，由定理1，EO 差距相应受界。 $\square$

---

## 五、定理与证明：置信度与公平性诊断

### 定理4（置信度与决策边界距离）

在假设1下，置信度 $c$ 和决策边界距离 $d$ 满足：
$$c = \sigma(\|w\| \cdot d)$$

因此 $c$ 关于 $d$ 严格单调递增。

### 定理4 证明

由定义：
$$c = \sigma(|s|) = \sigma(|w^\top z + b|), \quad d = \frac{|w^\top z + b|}{\|w\|}$$

因此 $|w^\top z + b| = \|w\| \cdot d$，代入得：
$$c = \sigma(\|w\| \cdot d)$$

由于 $\sigma$ 严格递增，$c$ 关于 $d$ 严格递增。 $\square$

### 引理3（决策边界距离与群组预测概率）

在假设1-2下，对于群组 $g$ 和标签 $l$：
$$\mathbb{E}[d \mid g, y=l] = \frac{|\mu'_g|}{\|w\|} + O(\sigma_g / \|w\|)$$

其中 $\mu'_g = w^\top \mu_{g,l} + b$，$\sigma_g = \sqrt{w^\top \Sigma_{g,l} w}$。

群组预测概率满足：
$$P(\hat{y}=1 \mid g, y=l) = \Phi\!\left(\frac{\mu'_g}{\sigma_g}\right) + O(1/\sqrt{n})$$

### 引理3 证明

由于 $s = w^\top z + b$ 是 sub-Gaussian 的，均值为 $\mu'_g$，方差为 $\sigma_g^2$：
$$d = \frac{|s|}{\|w\|}, \quad \mathbb{E}[d] = \frac{\mathbb{E}[|s|]}{\|w\|}$$

对于 sub-Gaussian 的 $s$，$\mathbb{E}[|s|] \approx |\mu'_g| + O(\sigma_g)$。

预测概率由 sub-Gaussian 集中不等式（Berry-Esseen 型近似）：
$$P(\hat{y}=1 \mid g, y=l) = P(s > 0) = \Phi\!\left(\frac{\mu'_g}{\sigma_g}\right) + O(1/\sqrt{n})$$

$\square$

### 定理5（置信度差异与 EO 差距）

在假设1-3下，定义群组置信度差异：
$$\Delta_c^{(l)} = |\mathbb{E}[c \mid g=0, y=l] - \mathbb{E}[c \mid g=1, y=l]|$$

则：
$$\Delta_c^{(l)} \leq \frac{\|w\|}{4} \cdot |\mathbb{E}[d \mid g=0, y=l] - \mathbb{E}[d \mid g=1, y=l]|$$

当 $\mu'_{g0} \approx 0$（低置信度群组）且 $|\mu'_{g1}| \gg 0$（高置信度群组）时，EO 差距的下界与置信度差异成正比：
$$\text{EO}^{(l)} \gtrsim \Phi\!\left(\frac{2\Delta_c}{\sigma_{g1}}\right) - 0.5$$

### 定理5 证明

**Step 1：用 Lipschitz 连续性界住 $\Delta_c$**

由定理4，$c = \sigma(\|w\| \cdot d)$。sigmoid 函数 $\sigma$ 是 $1/4$-Lipschitz 的。因此：
$$\Delta_c^{(l)} \leq \frac{1}{4} \cdot \|w\| \cdot |\mathbb{E}[d \mid g=0] - \mathbb{E}[d \mid g=1]|$$

（利用 Jensen 不等式将期望移入 $\sigma$）

**Step 2：将决策边界距离与预测概率关联**

由引理3：
$$P(\hat{y}=1 \mid g, y=l) = \Phi\!\left(\frac{\mu'_g}{\sigma_g}\right)$$

EO 差距为：
$$\text{EO}^{(l)} = \left|\Phi\!\left(\frac{\mu'_{g0}}{\sigma_{g0}}\right) - \Phi\!\left(\frac{\mu'_{g1}}{\sigma_{g1}}\right)\right|$$

**Step 3：分析系统性置信度差异的情形**

假设群组 $g=0$ 置信度系统性偏低：$\mu'_{g0} \approx 0$（样本靠近决策边界）。
群组 $g=1$ 置信度系统性偏高：$|\mu'_{g1}| \gg 0$（样本远离决策边界）。

则：
$$P(\hat{y}=1 \mid g=0, y=l) \approx \Phi(0) = 0.5$$
$$P(\hat{y}=1 \mid g=1, y=l) \approx \Phi\!\left(\frac{\mu'_{g1}}{\sigma_{g1}}\right) \approx 0 \text{ 或 } 1$$

因此：
$$\text{EO}^{(l)} \approx |0.5 - 0| = 0.5 \text{ 或 } |0.5 - 1| = 0.5$$

更精确地，利用置信度差异 $\Delta_c$ 和 Step 1 的 Lipschitz 界：
$$\Delta_d \geq \frac{4 \Delta_c}{\|w\|}$$

由引理3，$\Delta_d \approx |\mu'_{g0} - \mu'_{g1}|/\|w\|$。当 $\mu'_{g0} \approx 0$ 时：
$$\text{EO}^{(l)} \approx \Phi\!\left(\frac{|\mu'_{g1}|}{\sigma_{g1}}\right) - 0.5$$

由于 $|\mu'_{g1}| = \|w\| \cdot \mathbb{E}[d \mid g=1]$ 且 $\mathbb{E}[d \mid g=1] \geq \Delta_d/2$：
$$\text{EO}^{(l)} \gtrsim \Phi\!\left(\frac{\|w\| \Delta_d}{2\sigma_{g1}}\right) - 0.5 \geq \Phi\!\left(\frac{2\Delta_c}{\sigma_{g1}}\right) - 0.5$$

这表明置信度差异 $\Delta_c$ 越大，EO 差距的下界越大。 $\square$

> **注**：定理5建立了**定性**关联：群组间系统性置信度差异指示 EO 差距。定量界依赖于分布参数（$\sigma_g$, $\mu'_g$），随数据集和模型变化。核心逻辑链为：
> - 低置信度 ⟺ 靠近决策边界 ⟺ 预测概率接近 0.5
> - 高置信度 ⟺ 远离决策边界 ⟺ 预测概率接近 0 或 1
> - 群组间系统性置信度差异 ⟹ 预测概率系统性差异 ⟹ EO 差距

---

## 六、证明链路总结

### 第一部分：梯度对齐 → 表征偏差 → 公平性

| 步骤 | 定理/引理 | 结论 |
|------|----------|------|
| 1 | 定理1 | $\text{EO} \leq C \cdot \Delta_{\text{rep}} + \epsilon$ |
| 2 | 引理1 | $\|u\|^2 = \|g_0\|^2 + \|g_1\|^2 - 2\|g_0\|\|g_1\|\rho_l$（代数恒等式） |
| 3 | 定理2 | $\Delta^{(t+1)2} - \Delta^{(t)2} = -2\eta v \cdot u + \eta^2\|u\|^2$（精确等式） |
| 4 | 引理2 | 梯度对齐正则化不破坏收敛 |
| 5 | 定理3（主定理） | 梯度对齐约束 $\Delta_{\text{rep}}$，从而约束 EO |

### 第二部分：置信度 → 决策边界 → 公平性

| 步骤 | 定理/引理 | 结论 |
|------|----------|------|
| 1 | 定理4 | $c = \sigma(\|w\| \cdot d)$（置信度与决策边界距离单调递增） |
| 2 | 引理3 | $\mathbb{E}[d] \approx |\mu'_g|/\|w\|$，$P(\hat{y}=1) \approx \Phi(\mu'_g/\sigma_g)$ |
| 3 | 定理5 | $\Delta_c \leq \frac{\|w\|}{4}\Delta_d$，大 $\Delta_c$ ⟹ EO 下界大 |
